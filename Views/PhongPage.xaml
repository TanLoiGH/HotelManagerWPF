<Page x:Class="QuanLyKhachSan_PhamTanLoi.Views.PHONGPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F5F5F5">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh lọc trạng thái -->

        <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,20">

            <Button Content="Tất cả phòng" Margin="5"/>
            <Button Content="Phòng trống" Margin="5"/>
            <Button Content="Phòng đã đặt" Margin="5"/>
            <Button Content="Phòng dọn dẹp" Margin="5"/>

        </StackPanel>

        <!-- Danh sách phòng -->

        <ScrollViewer Grid.Row="1">

            <ItemsControl x:Name="DanhSachPhong">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Border Width="240"
        Height="150"
        Margin="12"
        Padding="12"
        CornerRadius="18"
        Background="White"
        MouseLeftButtonDown="Phong_Click">

                            <Border.Style>

                                <Style TargetType="Border">

                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </Setter.Value>
                                    </Setter>

                                    <Style.Triggers>

                                        <!-- hover animation -->

                                        <EventTrigger RoutedEvent="MouseEnter">

                                            <BeginStoryboard>

                                                <Storyboard>

                                                    <DoubleAnimation
Storyboard.TargetProperty="RenderTransform.ScaleX"
To="1.05"
Duration="0:0:0.15"/>

                                                    <DoubleAnimation
Storyboard.TargetProperty="RenderTransform.ScaleY"
To="1.05"
Duration="0:0:0.15"/>

                                                </Storyboard>

                                            </BeginStoryboard>

                                        </EventTrigger>

                                        <EventTrigger RoutedEvent="MouseLeave">

                                            <BeginStoryboard>

                                                <Storyboard>

                                                    <DoubleAnimation
Storyboard.TargetProperty="RenderTransform.ScaleX"
To="1"
Duration="0:0:0.15"/>

                                                    <DoubleAnimation
Storyboard.TargetProperty="RenderTransform.ScaleY"
To="1"
Duration="0:0:0.15"/>

                                                </Storyboard>

                                            </BeginStoryboard>

                                        </EventTrigger>

                                    </Style.Triggers>

                                </Style>

                            </Border.Style>

                            <StackPanel>

                                <!-- mã phòng -->

                                <TextBlock Text="{Binding MaPhong}"
           FontSize="20"
           FontWeight="Bold"/>

                                <TextBlock Text="{Binding LoaiPhong}"
           Foreground="Gray"/>

                                <!-- trạng thái -->

                                <Border Padding="4"
        CornerRadius="8"
        Margin="0,5"
        Background="{Binding MauTrangThai}">

                                    <TextBlock Text="{Binding TrangThai}"
           FontSize="12"
           Foreground="Black"/>

                                </Border>

                                <TextBlock Text="Chưa có thông tin"
           HorizontalAlignment="Center"
           Foreground="Gray"
           Margin="0,20,0,0"/>

                            </StackPanel>

                        </Border>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

    </Grid>
</Page>