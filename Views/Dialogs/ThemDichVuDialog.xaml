<!-- ThemDichVuDialog.xaml -->
<Window x:Class="QuanLyKhachSan_PhamTanLoi.Views.ThemDichVuDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Dịch Vụ" Width="460" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Danh sách dịch vụ -->
        <DataGrid x:Name="DichVuGrid" Grid.Row="0"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" FontSize="15"
                  BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên dịch vụ" Binding="{Binding TenDichVu}" Width="*"/>
                <DataGridTextColumn Header="Giá" Binding="{Binding GiaText}" Width="100"/>
                <DataGridTextColumn Header="ĐVT" Binding="{Binding DonViTinh}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Số lượng -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Số lượng:" FontSize="15" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <Border Grid.Column="2" CornerRadius="8" Style="{DynamicResource MacFluent.InputFrame}" Height="34">
                <TextBox x:Name="TxtSoLuong" Text="1" BorderThickness="0" Padding="10,0"
                         VerticalAlignment="Center" FontSize="15" TextAlignment="Right"/>
            </Border>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Hủy" Height="36" FontSize="15"
                    Click="BtnHuy_Click" Background="{DynamicResource Brush.SurfaceSecondary}" BorderThickness="0"/>
            <Button Grid.Column="2" Content="Thêm dịch vụ" Height="36" FontSize="15"
                    FontWeight="SemiBold"
                    Style="{DynamicResource MacFluent.ButtonAction.Add}"
                    Click="BtnChon_Click"/>
        </Grid>
    </Grid>
</Window>





